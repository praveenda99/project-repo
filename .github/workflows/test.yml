name: shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hellow world this is praveen"
      - name: multy line script 
        run : |
          node -v
          npm -v



