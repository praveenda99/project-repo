name: shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eecho "Hellow world"
      - name: multy line script 
        run : |
          node -v
          npm -v



